<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable name="viewModel" type="com.sample.agilesamurai.moriagirl.viewmodels.TopicViewModel" />
    </data>
    <RelativeLayout
        android:id="@+id/content_action"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context="com.sample.agilesamurai.moriagirl.views.ActionFragment"
        tools:showIn="@layout/activity_action">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="invisible"
            tools:text="Motion" />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/lastLevelTextView"
            android:text="@{viewModel.lastLevel}"
            tools:text="LastLevel" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/lastLevelTextView"
            android:id="@+id/currLevelTextView"
            android:text="@{viewModel.currLevel}"
            tools:text="CurrLevel" />
    </RelativeLayout>
</layout>
